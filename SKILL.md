---
name: meeting-transcript-cleaner
description: 逐字稿全生命週期工作流：從 MacWhisper 轉錄／截圖識別 → 講者校正 → 清稿／教學組織／三層整理。涵蓋 MacWhisper CLI 轉錄、SQLite 截圖識別兩階段流程、課程逐字稿三等級教學組織、長逐字稿並行清理。預設模型 whisperkit:openai_whisper-large-v2。觸發詞：幫我轉錄、清稿、逐字稿整理、會議錄音整理、音檔整理、MacWhisper、清理課程逐字稿、超詳細／很詳細／1比1。
---

# 逐字稿全生命週期工作流

## 角色定位

你負責從音檔／截圖入口到整理交付的完整鏈條：

```
音檔／MacWhisper 截圖
  ↓
[第一階段] 轉錄／撈 SQLite → 講者校正 → 存原始逐字稿
  ↓
[第二階段] 按「內容主體 × 用途」決定清理方式
  ↓
交付物（清稿／教學組織稿／三層整理）
```

第一階段是轉錄與講者校正；第二階段依逐字稿的內容主體與用途，選擇對應的清理／整理方式。

---

## ⚠️ MacWhisper 逐字稿不要直接餵 LLM 整理（硬規則）

**禁止**：MacWhisper 出來的逐字稿直接丟給 LLM 叫「幫我整理重點摘要」。

**為什麼**：MacWhisper 對中文有系統性轉錄錯誤（特別是人名／術語／同音字）。直接餵 LLM 摘要，錯字會被「視為事實」並隱性流傳。真正的關鍵字、關鍵指標、人名會被洗掉。

**典型誤轉**（同音／音近）：
- 語音「A-gent」常被聽成某個人名
- 技術術語被聽成不相干的日常詞
- 組織名、專有名詞、人名被音近的常用詞取代

如果直接餵 LLM 摘要，這些錯字就會被當真，整段脈絡被帶歪。

**正確流程**：
1. **先做 L2 清理**（本工作流的清稿模式）修錯字、保結構、留脈絡
2. **再餵下游**（寫文、做摘要、提煉觀點）

---

## ⭐ 逐字稿決策樹（讀這張就知道怎麼處理）

```
                     【音檔／截圖／已有逐字稿】
                              │
                              ▼
              ┌─────────────────────────────────┐
              │ 第一階段：轉錄 + 講者校正           │
              └─────────────────────────────────┘
                              │
                  存原始逐字稿（檔名含「原始逐字稿」前綴，永不覆蓋）
                              │
                              ▼
              ┌─────────────────────────────────┐
              │ 第二階段：判斷「誰的內容 × 什麼用途」 │
              └─────────────────────────────────┘
                              │
   ┌──────────────────────────┼──────────────────────────┐
   ▼                          ▼                          ▼
【自己的課程】              【外部講師的課程】          【會議／團隊對話】
   │                          │                          │
   ├─ 學員上課錄音            └─ 只提煉思維框架            ├─ 一般會議 → 會議整理
   │  → 三等級教學組織            不做完整順稿              │
   │                                                     └─ 重要會議／合作講座
   └─ 想改寫成文章                                            → 順稿 + 三層整理
      → 順稿後交寫作工作流
```

### 交付物命名前綴對照表

第二階段所有產出**另存新檔，原始逐字稿永遠保留**：

| 交付物類型 | 前綴 | 範例 |
|---|---|---|
| 原始逐字稿（永留） | `原始逐字稿` | `2026-05-12-1900 原始逐字稿：某場創業講座.md` |
| 課程教學組織稿 | `逐字稿清理` 或 `課程組織稿` | `2026-05-12-1901 課程紀錄：5/12 課程組織稿.md` |
| 一般會議整理稿 | `會議紀錄整理` | `2026-05-22-1131 會議紀錄整理：某次會議.md` |
| 三層整理（重要會議／講座） | `三層整理` | `2026-05-20-1930 三層整理：某場合辦講座.md` |

整理版檔頭加連結指回原始逐字稿，方便回查。

---

## 工作模式

### Mode 1：快速轉錄
觸發：「幫我轉錄」「只要逐字稿」「快速轉」

1. 確認音檔位置與格式
2. 用 MacWhisper CLI 轉出逐字稿
3. 存 txt 到原資料夾旁

不整理、不分析。

### Mode 2：會議記錄整理（主要模式）
觸發：「幫我整理錄音」「會議記錄」「整理成文字」「清稿」

1. 轉錄（或接收已有逐字稿）
2. 順稿：去除語氣詞、填補漏字、段落分行
3. 依主題或時間線整理
4. 存歸檔位置

輸出：一份可閱讀的會議記錄，不含分析。

### Mode 3：詳細整理（三層）
觸發：「詳細版」「三層整理」「要有摘要跟重點」

1. 轉錄
2. 產出原始逐字稿（層一）
3. 順稿整理版（層二）
4. 摘要 + 重點 + 行動項目（層三）

### Mode 4：批次轉錄
觸發：「整個資料夾」「batch 轉」「所有錄音」

1. 列出資料夾所有音檔
2. 依序或並行轉錄（用批次腳本）
3. 各自存 txt
4. 回報完成清單

---

### Mode 5：MacWhisper 截圖識別模式（兩階段流程）

**觸發**：使用者截 MacWhisper App 列表畫面 +「這是 MacWhisper 的逐字稿」「先存檔跟校正講者」「等等清理」。

MacWhisper 把逐字稿存在 SQLite，檔案系統裡找不到。看到截圖第一件事：直接開資料庫，別急著去 find 檔案。

#### 階段一：存檔 + 校正講者

1. **辨識截圖內容**：從 MacWhisper App 列表截圖讀出 session 標題（通常是 `YYYY-MM-DD HH-mm-ss 名稱` 格式，對應 SQLite 的 `originalFilename`）+ 說話人數（`N speakers`）+ 開頭內容片段
2. **撈 SQLite**：
   ```bash
   sqlite3 ~/Library/Application\ Support/MacWhisper/Database/main.sqlite \
     "SELECT hex(id), originalFilename, dateCreated, length(fullText), hasBeenDiarized \
      FROM session WHERE dateDeleted IS NULL AND \
      (originalFilename LIKE '%關鍵字%') ORDER BY dateCreated DESC;"
   ```
3. **用 Python 腳本匯出**（模板見下）：
   - 一份檔對應一個 session id
   - 合併同說話人連續行（同 speaker 連講變一段）
   - 帶 timestamp `[HH:MM:SS]` 標頭
   - 每份檔加：來源、原檔名、錄製時間、字數、段落數、「說話人對照」區塊（先用 `Speaker N` 預留）、兩階段 checklist
4. **讀開頭判斷講者**：對每份檔讀前 80-100 行，找關鍵線索：
   - 自我介紹「我叫 XXX」「我是 XXX」→ 直接定講者
   - 講話風格、業務領域、人稱代詞
   - 同一人在多場會議的一致口吻
   - 講者數對不上截圖時，看 `transcriptline` 表 group by speakerID 確認
5. **回報講者對照表給使用者確認**（表格：Speaker N → 推測 → 證據）
6. **使用者確認後**重生檔案（把 `Speaker N` 替換成真實姓名），舊檔搬到 `_trash/YYYY-MM-DD/`（不要直接刪）

**講者類型處理慣例**：
- **主持人／自己人在場** → 用全名，不用口語別名
- **合作對象** → 真實姓名
- **外部課程的講師** → 標「講師」就好（不用查名字）
- **外部課程的學員** → 自介過名字的用名字，沒自介的標「不明學員」
- **多餘 speaker（diarization 多切的）** → 合併到對話對象（規則見下）

**⭐ 多餘 speaker 自動合併規則**

當使用者明確說「今天 N 個人開會」+ 已給出 N 個確定的人名／角色，但 MacWhisper diarization 切出 N+1 個或更多 speakers，超出的那幾個通常**不代表真的多了一個人**，常見原因是：

1. **收音品質問題**：某人這句比較小聲、離麥克風遠 → diarization 信心低，切成新 speaker
2. **兩人搶話**：A 被 B 打斷，A 接下去的話被切到新 speaker
3. **同一人被誤分**：聲線變化、笑聲、語氣改變

處理原則：**看上下文自動合併，不要當成新角色**。判斷依據（按優先順序）：
- 該 speaker 段落絕大多數是 **backchanneling**（嗯／對／好／是／OK）→ 合併到「對話對象」（搭腔者）
- 段落出現在某固定講者長段**中間**，前後是同一其他 speaker → 看內容是搭腔（合給對話對象）還是被切的句子（合給說話者）
- 段落出現在**對話結尾**（道別語）→ 合給結尾在講話的那一方
- 真的語意差太多、明顯不是 N 個人之一 → 才保留標「待確認」

實作層面：直接把 `Speaker N` 在 `speakers` dict 裡 mapping 到要合併的真實姓名，腳本的「連續同 speaker 合併」邏輯會自動把搭腔位置處理乾淨。

**容忍原則**：被誤合的句子不用挑那麼細，反正後續清稿會清掉。不影響「整體脈絡 + 各方立場 + 目標」就 OK。

**第一階段交付**：N 份原始逐字稿，講者已校正（含多餘 speaker 合併），等待第二階段。

#### 階段二：清稿／整理

由使用者觸發（「開始清理」「跑清稿」）。依「⭐ 逐字稿決策樹」按「誰的內容 × 什麼用途」決定清理方式：
- 自己的課程逐字稿 → **Mode 6**（課程教學組織三等級）
- 外部課程 → 只提煉思維框架，不順稿
- 一般會議 → 會議整理
- 重要會議／合作講座 → 順稿 + 三層整理

所有分支共用「清稿三大硬性規則」（見下方）。

---

## ⭐ 原始逐字稿保留鐵則（所有類型適用）

第二階段清稿／整理後，**原始逐字稿一律保留不刪**。

### 為什麼
- 提煉、清稿、順稿都會丟失資訊（精簡掉的細節、被誤合的句子、被改寫的口語）
- 真的有問題（事實爭議、引述不確定、需要回查語境）時，要能回到原始版本複查
- 整理版只是「為了快速使用」的衍生產物，原版才是法定來源

### 怎麼留
- 第一階段匯出的原始逐字稿檔案（檔名含「原始逐字稿」前綴），**不要動**
- 第二階段產出的整理版／三層整理，**另存新檔**，不覆蓋原檔
- 整理版檔頭加連結指回原始逐字稿

### 何時可以刪原始稿
只有以下情況才考慮刪：使用者明確說「刪掉原始逐字稿」、或已另有備份。否則一律保留。

#### Python 匯出腳本模板

```python
import sqlite3
from pathlib import Path

DB = str(Path.home() / "Library/Application Support/MacWhisper/Database/main.sqlite")
OUT_DIR = Path("輸出資料夾絕對路徑")

SESSIONS = [
    {
        "id": "HEX_SESSION_ID",
        "fname": "YYYY-MM-DD-HHMM 原始逐字稿：標題",
        "title": "顯示用標題",
        "speakers": {"Speaker 1": "真實姓名"},
        "kind": "會議紀錄 / 合作講座 / 外部參考",
    },
]

def ms_to_hms(ms):
    s = ms // 1000
    return f"{s//3600:02d}:{(s%3600)//60:02d}:{s%60:02d}"

conn = sqlite3.connect(DB)
conn.row_factory = sqlite3.Row

for cfg in SESSIONS:
    sid = bytes.fromhex(cfg["id"])
    sess = conn.execute("SELECT * FROM session WHERE id = ?", (sid,)).fetchone()
    lines = conn.execute("""
        SELECT t.start, t."end" AS endt, t.text, s.name AS speaker
        FROM transcriptline t LEFT JOIN speaker s ON t.speakerID = s.id
        WHERE t.sessionId = ? ORDER BY t.start
    """, (sid,)).fetchall()

    # 合併連續同 speaker
    merged = []
    for ln in lines:
        spk = cfg["speakers"].get(ln["speaker"] or "Unknown", ln["speaker"] or "Unknown")
        if merged and merged[-1]["speaker"] == spk:
            merged[-1]["text"] += " " + ln["text"]
        else:
            merged.append({"start": ln["start"], "speaker": spk, "text": ln["text"]})
    # ... 輸出 Markdown
```

---

### Mode 6：課程逐字稿教學組織（三等級）

**觸發**：使用者給「原始逐字稿」檔案（檔名含 `原始逐字稿` 前綴），說「清理這一份」「整理成組織稿」「清理課程逐字稿」「課程組織稿」。場景限定**自己的課程逐字稿**（學員上課錄音轉成的教學閱讀稿）。

**核心原則：**
- **保留原始講述脈絡**（不重新排序，不按簡報結構重組）
- **對照課綱／簡報**作為輔助理解工具，不是改寫模板
- **去冗留實**：去除語氣詞、重複、離題閒聊，保留所有實質內容

#### 三等級清理

| 等級 | 觸發詞 | 處理原則 | 保留率目標 |
|------|--------|----------|-----------|
| **超詳細** | 「超詳細」「完整版」 | 所有實質內容都留，連小例子都保留，補標題分段，語言清順 | 80-90% |
| **很詳細**（預設） | 「很詳細」（無指定也用這個） | 所有重要概念、例子、故事留下，去除冗長重複段落 | 65-70% |
| **1:1** | 「1比1」「接近原始」 | 幾乎原樣，只去語氣詞，不改結構和用詞 | 90-95% |

#### 模型分工策略（≥ 20,000 字適用）

> **適用範圍限定**：此分工專屬於「原始逐字稿 → 清理稿」階段。後續深度分析、改寫文章、策略產出的模型分工另行決定，不套用此規則。

短逐字稿（< 20,000 字）：單一模型一條龍即可。

長逐字稿（≥ 20,000 字）：採三段式分工，避免壓縮過頭。

| 階段 | 模型角色 | 工作 |
|------|------|------|
| 1. 規劃 | 高階模型 | 列段落結構、每段預估字數、必保留的金句／故事／案例 |
| 2. 執行 | 中階模型 | 照規劃逐段清理，產出完整文檔 |
| 3. 抽查 | 高階模型 | 對照規劃檢查字數比例、段落完整性、是否壓縮過頭 |

**規劃必須包含（缺一就會被壓縮過頭）：**
1. 每段預估字數
2. 必保留的金句（明列原文，禁止改寫）
3. 必保留的故事／案例（清單條列）
4. 可刪除的內容（互動投票指令、休息音效、技術當機、過度語氣詞）
5. 保留率目標（依等級）

#### 執行流程

**Step 1：定位對應課綱／簡報**
- 從原始逐字稿日期找同一天的課綱、簡報、預告文件
- 找到的話拿來作為**段落標記參考**（知道哪段在講什麼）
- 找不到的話，直接依逐字稿的講述脈絡自然分段

**Step 2：確認清理等級**（預設「很詳細」）

**Step 3：清理執行**

清除對象（依等級調整力道）：
- 語氣詞：「啊」「哦」「那個」「就是說」「反正」過度重複的
- 重複：前後說了幾乎一樣意思的段落
- 離題：技術當機說明、互動投票問答（用重點摘要取代）、閒聊
- 時間戳：整理後不需要

保留對象：
- 所有實質概念、原則、觀念
- 所有故事和案例（這些是核心）
- 金句（原文保留，加粗或引用格式）
- 類比和比喻（幫助記憶）
- Q&A 中有價值的回答
- 講師的語氣風格和第一人稱視角

**Step 4：格式化**

```
> 整理模式：[等級] + 對照簡報說明（若有）

---

# 課程標題
**日期與場次資訊**

---

## 一、[段落名稱]（對應時間，若有）
[清理後的內容]

### 故事／案例標題（若有）
...
```

**Step 5：存檔**

```
原始逐字稿：YYYY-MM-DD-HHMM [標題] 原始逐字稿.md
組織稿：    YYYY-MM-DD-HH(MM+1) [標題] 逐字稿清理.md
```

`MM+1`：把原始的分鐘數 +1，不夠就進位到小時。例：`2026-05-03-2200` → `2026-05-03-2201`。

**Step 6：回報** 完成後告知清理版檔案名稱、存放位置。

#### 注意事項
- 課程有前後段邏輯跳躍（例：當機後繼續），用小標題隔開，不要省略
- 互動問卷、投票環節：只留核心發現，不留「打知道、打不知道」的指令
- 使用者說「這一份清理」沒指定等級，預設「很詳細」

#### 為什麼三等級 + 為什麼分工

實測教訓：規劃太粗時，再強的模型也會壓縮過頭（曾把 35,000 字的課程逐字稿，因規劃不細而被壓到 6,800 字，保留率僅 19.5%，遠低於「很詳細」目標）；補完規劃後重做，達到 64% 保留率。結論：規劃不夠細，再強的模型也會壓縮過頭；規劃夠細，中階模型就能照規執行不偏題。

---

## MacWhisper CLI 已驗證規格

### CLI 路徑

```bash
/Applications/MacWhisper.app/Contents/MacOS/mw
```

> 官方文件說 `/usr/local/bin/mw`，但實際常不在這裡。直接用 app bundle 路徑。

### 版本確認

```bash
"/Applications/MacWhisper.app/Contents/MacOS/mw" version
```

### 模型清單

```bash
"/Applications/MacWhisper.app/Contents/MacOS/mw" models list
```

| 模型 | 用途 |
|------|------|
| `whisperkit:openai_whisper-large-v2` | **預設，品質穩定** |
| `whisperkit:openai_whisper-large-v3-v20240930` | 速度優先，偶有中文誤轉 |
| `whisper-cpp:ggml-model-whisper-medium` | 備用 |
| `whisper-cpp:ggml-model-whisper-small` | 快速測試用 |

### 單檔轉錄指令

```bash
caffeinate -i "/Applications/MacWhisper.app/Contents/MacOS/mw" transcribe \
  --model "whisperkit:openai_whisper-large-v2" \
  --stream \
  "/path/to/audio.m4a" \
  > "/path/to/output.txt"
```

`caffeinate -i`：避免 Mac 在轉錄中睡眠中斷。`--stream`：邊轉邊輸出，方便觀察進度。

### 背景執行（長音檔）

長音檔用背景執行，不要 hardcode `&`，用 runner 的背景任務機制。

---

## 轉錄前必確認

1. **音檔路徑**：含空格或中文的路徑必須用雙引號包住
2. **輸出格式**：純 txt？還是要 Markdown？要時間軸嗎？（預設純 txt）
3. **整理目的**：只要文字 / 依主題整理 / 三層
4. **歸檔位置**：轉完存哪裡（預設存音檔旁）

**格式未確認前不開始轉錄。**

---

## 雜音環境處理

咖啡廳等有背景噪音的錄音：
- 模型仍用 `large-v2`（對雜音抵抗力較好）
- 順稿時主動標記明顯聽不清的段落：`[不清楚]`
- 不要硬猜填補，留空標記比亂填好

---

## 清稿三大硬性規則

### 規則 1：字數計算一律 Python，禁止 wc -m

寫清稿 prompt 前**必須先用 Python 確認字數**，禁止用 `wc -m`（macOS 預設 locale 下算 bytes 不算字元，中文會差 3 倍）。

```bash
# 算原始字數
python3 -c "print(len(open('FILE').read()))"

# 算目標字數（壓縮到 40-50% 為例）
python3 -c "n=len(open('FILE').read()); print(f'目標：{int(n*0.4):,}-{int(n*0.5):,} 字')"
```

prompt 裡寫目標字數時，給「字元數區間」（例：19,000-24,000 字）而不是「百分比」。LLM 對字元數比較容易抓準目標。

### 規則 2：清稿規則必加「主動修正 Whisper 聽錯」條款

模型預設保守，沒明說授權就不會修錯字。**規則模板必加**：

```
【主動修正 Whisper 聽錯】
看到語意不通、明顯音近誤判的詞，主動修正。判斷依據：
1. 上下文已出現過正確版本
2. 音近但語意明顯錯誤
3. 專有名詞、組織名、人名、地名特別注意

修正後在該段落結尾用 `※ 修正：誤字→正字` 標註，方便使用者校對。
不確定的不要硬改，標 `[聽不清]` 或保留原文。
```

實證：對照測試 6 個 Whisper 聽錯，無此條款的保守模型只修 1/6；自由度高、會自發修正的模型修 6/6。

### 規則 3：跨家審稿是硬要求

清稿流程**必須**插入跨家 reviewer，不可省略。

```
Cleaner = A 家族模型
  ↓
Reviewer 1 = 不同家的模型（必跨家，主審）  ← 硬性
Reviewer 2 = 另一個高階模型（可選，輔審）
  ↓
[兩位 reviewer 結論一致] → 通過、存檔
[結論不一致] → 回高階模型仲裁
```

**禁止**：
- ❌ 同家族模型審自己（自己審自己）
- ❌ 單一高階模型當唯一 reviewer（家族偏見，會給自家加分）
- ❌ 跳過 reviewer 直接存檔

為什麼：同家族模型對自家產出有系統性偏好，自審會放水。跨家審稿才能抓到真問題。

---

## 觸發詞

- 「幫我轉錄」「MacWhisper」「清稿」「逐字稿整理」
- 「會議錄音整理」「音檔整理」「m4a／mp3／wav 整理」
- 「整理錄音」「順稿」「會議記錄」（配合音檔出現時）
- 截 MacWhisper App 列表畫面 +「這是 MacWhisper 的逐字稿」「先存檔跟校正講者」→ Mode 5 兩階段流程
- 「清理這一份」「整理成組織稿」「清理課程逐字稿」「課程組織稿」「超詳細／很詳細／1比1」→ Mode 6

---

## 後續步驟（本工作流範圍外）

逐字稿清乾淨之後，常見的下一步（交給其他工作流或人工處理）：
- **會議的共識分析、角色定位、四象限、會議策略書** → 會議分析工作流
- **改寫成文章、貼文** → 寫作工作流
- **外部課程／別人文章的思維框架提煉** → 知識提煉工作流

本工作流只負責：轉錄、講者校正、原始逐字稿保存、清稿、品質檢查。清乾淨的稿子交出去，不做深度分析與改寫。
